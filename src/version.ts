// One product release version for the plugin manifest, skill, MCP server, and
// embedded App. Change it only through `npm run release:version -- <semver>`.
export const PROJECT_MANAGER_VERSION = '1.12.1' as const;
