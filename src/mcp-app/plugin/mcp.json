{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "project-manager": {
      "type": "stdio",
      "command": "node",
      "args": ["${PLUGIN_ROOT}/skills/project-manager/scripts/project-manager-mcp.js"],
      "cwd": "${PLUGIN_ROOT}"
    }
  }
}
