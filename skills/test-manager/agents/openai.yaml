interface:
  display_name: "Test Manager"
  short_description: "Assess QA state and recommend the next action"
  default_prompt: "Use $test-manager to manage this workspace's QA tests, state the current assessment, and recommend exactly one next action when a handoff remains."
