interface:
  display_name: "Project Manager"
  short_description: "Assess delivery and recommend the next action"
  default_prompt: "Use $project-manager to assess this project's delivery state and recommend exactly one next action when a handoff remains."
